import 'package:flutter/material.dart';

import '../../../core/theme/widgets/proximamente_screen.dart';

class ReportesStubScreen extends StatelessWidget {
  const ReportesStubScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const ProximamenteScreen(titulo: 'Reportes', icono: Icons.bar_chart_outlined);
  }
}
